{
  "name": "pickupcg-web",
  "version": "1.0.0",
  "description": "Pickup landing page — pickupcg.com",
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'Static site — no build needed'"
  }
}
